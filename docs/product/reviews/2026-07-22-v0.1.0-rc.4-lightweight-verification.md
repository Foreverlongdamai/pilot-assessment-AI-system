# `v0.1.0-rc.4` Lightweight Release Candidate Verification

| Field | Value |
|---|---|
| Date | 2026-07-22 |
| Candidate | `v0.1.0-rc.4` |
| Tagged commit | `e14232f448a9ef9609860b894ea26fba43d59ba8` |
| Verification mode | approved lightweight differential gate |
| Engineering result | `PASS` |
| User acceptance | `pending` |
| Scientific status | starter `formal_run_authorized=false` |

## 1. Scope

RC.4 packages D-088 active Save All, D-089 persistent canonical/Raw Input Family dragging and
D-091 Model Studio toolbar clarification. It preserves the RC.3 Assessment, icon, global delete and
portable-root corrections. In accordance with the user's direction, this gate does not repeat the
full repository suite or the 281-page rendered-manual inspection. Preserved M8E/RC.3 evidence remains
historical evidence; this record verifies the RC.4 delta and the exact new archive.

## 2. Source and focused checks

| Check | Result |
|---|---|
| Desktop unit tests | `114 passed / 0 failed` |
| Desktop protocol contract tests | `4 passed / 0 failed` |
| Focused Python layout/sidecar tests | `8 passed / 0 failed` |
| Documentation/release tests | `17 passed / 0 failed` |
| Ruff on touched Python validation/test files | `PASS` |
| Released manual-source validation | `24/24` language variants, `0` pending screenshots |
| x64 Release build | `0 warnings / 0 errors` |
| RC.4 Model Studio screenshots | Chinese and English recaptured against UI source `ff3df364cc99e7c2be292791a2094e0d3cc08c115d5d61c00cb9b944c1613854` |

The candidate was built from a clean worktree and an annotated `v0.1.0-rc.4` tag that peels to the
tagged commit. The current system source was closed and clean before capture.

## 3. Candidate facts

| Fact | Value |
|---|---|
| Package directory bytes | `802,314,895` |
| ZIP bytes | `291,239,082` |
| ZIP SHA-256 | `a5fe85b6ce24dab76acff1d6b840a9fa2b5498e7f511ebb56d228db1c422f7f4` |
| Root surface | `8 directories / 2 files / 1 launcher`; WinUI payload under `app/` |
| Checksummed files | `4,335` |
| Released DOCX | `24` |
| Candidate screenshots | `10`; only the affected bilingual Model Studio pair was recaptured |
| Editable backend source files | `301` |
| System model | `54` nodes / `2` schemes / clean edit session |
| System model identity | `79efc59cb38242a7edfa1c85a5311729c40a769bcbf7256b2f5f1d5cb0400a1e` |

The release builder's normal package verifier passed, including its bounded operator-extension and
micro assessment smoke. No additional standalone editable-source, operator-extension or assessment
matrix was repeated for RC.4.

## 4. Repository-external verification

The exact ZIP was extracted to a disposable directory outside the repository and verified with a
restricted `PATH`:

- archive hash and delivery record matched;
- privacy scan covered `24` DOCX files and `360` DOCX XML members;
- root layout, manifests, SBOM, released manuals and all `4,335` checksummed files passed;
- the private Python runtime imported the packaged editable backend and the sidecar reached `ready`;
- the captured software-wide model remained `54` nodes / `2` schemes with a clean edit session;
- the sole root `PilotAssessment.exe` launched the real WinUI desktop and its packaged sidecar.

## 5. Distribution and boundary

`dist/releases` now contains only the RC.4 directory, ZIP, `.zip.sha256` and delivery JSON. RC.1,
RC.2 and RC.3 source tags and review records remain immutable; only their obsolete local binary
cache was removed.

This is a verified engineering candidate for independent user testing, not an accepted final
release and not a claim that the starter Evidence, BN topology, thresholds or CPTs are scientifically
valid. Only operation of this exact ZIP may close `user_acceptance=pending`.
