# `v0.1.0-rc.1` User Acceptance Result

| Field | Recorded value |
|---|---|
| Candidate | `v0.1.0-rc.1` |
| Date | 2026-07-21 |
| Reviewer | Product owner/user on Windows |
| Result | `changes-required` |
| Scientific status | unchanged: `formal_run_authorized=false` |

## Observation

The extracted product root exposed 94 directories and 374 files. WinUI/.NET DLLs, WinMD files,
framework and language-resource directories obscured the intended semantic entry points such as
`backend/`, `system/`, `runtime/`, `developer/` and `docs/`.

## Required correction

- contain the complete desktop runtime payload under `app/`;
- provide one clear launcher in the product root;
- preserve the visible semantic directories for editable Python, the global model library,
  private runtime, developer materials and documentation;
- retain the unpack-and-run experience and do not require manual backend/database startup.

## Status boundary

This record does not alter the RC.1 tag or historical verification evidence. RC.1 remains a
traceable rejected candidate. The correction must be implemented, tagged, packaged and verified
as RC.2, whose user acceptance begins again at `pending`.

